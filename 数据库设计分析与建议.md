# 古代算术计数游戏小程序 - 数据库设计分析与建议

## 📋 快速参考：游戏进度记录表

### 🎯 核心问题：哪个表记录玩家游戏进度？

**答案：主要使用 `player_level_unlock` 表**

| 表名 | 作用 | 记录内容 | 使用场景 |
|------|------|---------|---------|
| **`player_level_unlock`** ⭐ | **游戏进度汇总表** | 每个玩家每个关卡一条记录<br>- 是否解锁<br>- 是否完成<br>- 最佳分数<br>- 完成次数<br>- 完成时间 | 查询玩家进度<br>判断关卡解锁状态<br>显示关卡完成情况 |
| **`game_record`** | 游戏历史明细表 | 每次游戏一条记录<br>- 游戏时间<br>- 使用步数<br>- 游戏分数<br>- 通关状态 | 查看历史游戏记录<br>排行榜统计<br>游戏数据分析 |

**关系说明：**
- `player_level_unlock` = 进度汇总（每个关卡一条）
- `game_record` = 历史明细（每次游戏一条）
- 完成游戏时：先插入 `game_record`，再更新 `player_level_unlock`

---

## 一、现有数据库表分析

### 现有表结构
1. `game_levels` - 游戏关卡表
2. `game_record` - 游戏记录表  
3. `game_achievements` - 游戏成就表
4. `achievements` - 玩家成就表
5. `gamer` - 游戏玩家表
6. `props` - 道具商店表
7. `purchase_record` - 购买记录表

### 现有表的问题

#### 1. `game_levels` 表缺少关键字段
- ❌ **缺少赛道字段**：无法区分关卡属于哪个赛道（结绳计数、筹算、珠算、综合）
- ❌ **缺少关卡顺序字段**：无法控制关卡显示顺序和难度递增
- ❌ **缺少解锁条件字段**：无法定义关卡解锁规则
- ❌ **缺少关卡类型字段**：无法区分不同类型的交互方式

#### 2. 缺少核心业务表
- ❌ **赛道表（tracks/channels）**：管理四大赛道信息
- ❌ **关卡解锁状态表**：记录玩家关卡解锁进度
- ❌ **勋章/徽章表**：管理勋章信息
- ❌ **玩家勋章表**：记录玩家获得的勋章
- ❌ **文化科普内容表**：存储通关后的文化小贴士

#### 3. `game_record` 表需要优化
- ⚠️ 缺少赛道关联字段
- ⚠️ 缺少关卡完成状态（是否通关）

---

## 二、需要新增的数据库表设计

### 1. 赛道表（game_tracks）
```sql
DROP TABLE IF EXISTS `game_tracks`;
CREATE TABLE `game_tracks` (
  `track_id` int(11) NOT NULL AUTO_INCREMENT COMMENT '赛道ID',
  `track_name` varchar(64) NOT NULL COMMENT '赛道名称（结绳计数/筹算/珠算/综合）',
  `track_code` varchar(32) NOT NULL UNIQUE COMMENT '赛道代码（rope/counting_rods/abacus/comprehensive）',
  `track_icon` varchar(255) COMMENT '赛道图标',
  `track_description` text COMMENT '赛道描述',
  `sort_order` int(11) DEFAULT 0 COMMENT '排序顺序',
  `is_enabled` tinyint(1) DEFAULT 1 COMMENT '是否启用',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`track_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='游戏赛道表';

-- 初始化四大赛道数据
INSERT INTO `game_tracks` VALUES 
(1, '结绳计数', 'rope', '/static/tracks/rope.png', '通过打结的方式记录数字，体验古代计数智慧', 1, 1, NOW(), NOW()),
(2, '筹算', 'counting_rods', '/static/tracks/counting_rods.png', '使用算筹进行计数和运算，感受古代数学工具', 2, 1, NOW(), NOW()),
(3, '珠算', 'abacus', '/static/tracks/abacus.png', '操作算盘进行计算，学习传统计算方式', 3, 1, NOW(), NOW()),
(4, '综合挑战', 'comprehensive', '/static/tracks/comprehensive.png', '综合运用三种计数方式，挑战更高难度', 4, 1, NOW(), NOW());
```

### 2. 修改 `game_levels` 表
```sql
-- 添加赛道关联字段
ALTER TABLE `game_levels` 
ADD COLUMN `track_id` int(11) DEFAULT 0 COMMENT '所属赛道ID' AFTER `game_levels_id`,
ADD COLUMN `level_order` int(11) DEFAULT 0 COMMENT '关卡顺序（同一赛道内）' AFTER `track_id`,
ADD COLUMN `unlock_condition` varchar(255) COMMENT '解锁条件（如：需要完成上一关卡）' AFTER `level_order`,
ADD COLUMN `level_type` varchar(32) DEFAULT 'interactive' COMMENT '关卡类型（interactive-交互/quiz-问答）' AFTER `unlock_condition`,
ADD COLUMN `target_number` int(11) DEFAULT 0 COMMENT '目标数字（需要达到的计数结果）' AFTER `target_number`,
ADD INDEX `idx_track_order` (`track_id`, `level_order`);

-- 添加外键约束（可选）
-- ALTER TABLE `game_levels` ADD CONSTRAINT `fk_game_levels_track` FOREIGN KEY (`track_id`) REFERENCES `game_tracks` (`track_id`);
```

### 3. 关卡解锁状态表（player_level_unlock）⭐ **核心游戏进度表**

**这是记录玩家游戏进度的主要表**，每个玩家在每个关卡只有一条记录。

```sql
DROP TABLE IF EXISTS `player_level_unlock`;
CREATE TABLE `player_level_unlock` (
  `unlock_id` int(11) NOT NULL AUTO_INCREMENT COMMENT '解锁记录ID',
  `gamer_id` int(11) NOT NULL COMMENT '玩家ID',
  `game_levels_id` int(11) NOT NULL COMMENT '关卡ID',
  `track_id` int(11) NOT NULL COMMENT '赛道ID',
  `is_unlocked` tinyint(1) DEFAULT 0 COMMENT '是否已解锁（0-未解锁，1-已解锁）',
  `is_completed` tinyint(1) DEFAULT 0 COMMENT '是否已完成（0-未完成，1-已完成）',
  `best_score` double(8,2) DEFAULT 0 COMMENT '最佳分数',
  `complete_times` int(11) DEFAULT 0 COMMENT '完成次数',
  `first_complete_time` datetime COMMENT '首次完成时间',
  `last_complete_time` datetime COMMENT '最后完成时间',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`unlock_id`),
  UNIQUE KEY `uk_gamer_level` (`gamer_id`, `game_levels_id`),
  INDEX `idx_gamer_track` (`gamer_id`, `track_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='玩家关卡解锁状态表';
```

**使用场景：**
- ✅ 查询玩家所有关卡进度：`SELECT * FROM player_level_unlock WHERE gamer_id = ?`
- ✅ 检查关卡是否解锁：`SELECT is_unlocked FROM player_level_unlock WHERE gamer_id = ? AND game_levels_id = ?`
- ✅ 更新关卡完成状态：通关后更新 `is_completed = 1`, `best_score`, `complete_times` 等
- ✅ 判断下一关卡解锁条件：检查上一关卡 `is_completed = 1`

**与 `game_record` 表的关系：**
- `player_level_unlock`：**汇总表**，每个关卡一条记录，记录总体进度
- `game_record`：**明细表**，每次游戏一条记录，记录历史游戏数据
- 当玩家完成游戏时：
  1. 在 `game_record` 中插入一条游戏记录
  2. 更新 `player_level_unlock` 中的进度信息（如果通关则更新 `is_completed`、`best_score` 等）

### 4. 勋章表（medals）
```sql
DROP TABLE IF EXISTS `medals`;
CREATE TABLE `medals` (
  `medal_id` int(11) NOT NULL AUTO_INCREMENT COMMENT '勋章ID',
  `medal_name` varchar(64) NOT NULL COMMENT '勋章名称',
  `medal_code` varchar(32) NOT NULL UNIQUE COMMENT '勋章代码',
  `medal_icon` varchar(255) COMMENT '勋章图标',
  `medal_description` text COMMENT '勋章描述',
  `unlock_condition` varchar(255) COMMENT '解锁条件描述',
  `track_id` int(11) DEFAULT 0 COMMENT '关联赛道ID（0表示全赛道）',
  `level_id` int(11) DEFAULT 0 COMMENT '关联关卡ID（0表示不关联特定关卡）',
  `sort_order` int(11) DEFAULT 0 COMMENT '排序顺序',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`medal_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='勋章表';

-- 示例勋章数据
INSERT INTO `medals` VALUES 
(1, '结绳新手', 'rope_beginner', '/static/medals/rope_beginner.png', '完成结绳计数赛道第一关', '完成结绳计数赛道第一关', 1, 0, 1, NOW(), NOW()),
(2, '筹算达人', 'counting_rods_master', '/static/medals/counting_rods_master.png', '完成筹算赛道所有关卡', '完成筹算赛道所有关卡', 2, 0, 2, NOW(), NOW()),
(3, '珠算高手', 'abacus_expert', '/static/medals/abacus_expert.png', '完成珠算赛道所有关卡', '完成珠算赛道所有关卡', 3, 0, 3, NOW(), NOW()),
(4, '全能冠军', 'all_champion', '/static/medals/all_champion.png', '完成所有赛道', '完成所有赛道', 0, 0, 4, NOW(), NOW());
```

### 5. 玩家勋章表（player_medals）
```sql
DROP TABLE IF EXISTS `player_medals`;
CREATE TABLE `player_medals` (
  `player_medal_id` int(11) NOT NULL AUTO_INCREMENT COMMENT '玩家勋章ID',
  `gamer_id` int(11) NOT NULL COMMENT '玩家ID',
  `medal_id` int(11) NOT NULL COMMENT '勋章ID',
  `unlock_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '解锁时间',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`player_medal_id`),
  UNIQUE KEY `uk_gamer_medal` (`gamer_id`, `medal_id`),
  INDEX `idx_gamer` (`gamer_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='玩家勋章表';
```

### 6. 文化科普内容表（culture_tips）
```sql
DROP TABLE IF EXISTS `culture_tips`;
CREATE TABLE `culture_tips` (
  `tip_id` int(11) NOT NULL AUTO_INCREMENT COMMENT '科普内容ID',
  `track_id` int(11) NOT NULL COMMENT '关联赛道ID',
  `level_id` int(11) DEFAULT 0 COMMENT '关联关卡ID（0表示赛道通用）',
  `tip_title` varchar(128) COMMENT '科普标题',
  `tip_content` text COMMENT '科普内容（一句话）',
  `tip_image` varchar(255) COMMENT '配图路径',
  `sort_order` int(11) DEFAULT 0 COMMENT '排序顺序',
  `is_enabled` tinyint(1) DEFAULT 1 COMMENT '是否启用',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`tip_id`),
  INDEX `idx_track_level` (`track_id`, `level_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文化科普内容表';

-- 示例科普内容
INSERT INTO `culture_tips` VALUES 
(1, 1, 0, '结绳计数', '结绳计数是古代最早的计数方式之一，通过绳结的数量和位置来记录数字', '/static/culture/rope_tip.png', 1, 1, NOW(), NOW()),
(2, 2, 0, '筹算', '算筹是中国古代重要的计算工具，用不同长度的棍子表示不同的数字', '/static/culture/counting_rods_tip.png', 1, 1, NOW(), NOW()),
(3, 3, 0, '珠算', '算盘是古代最先进的计算工具，至今仍在一些地方使用', '/static/culture/abacus_tip.png', 1, 1, NOW(), NOW());
```

### 7. 修改 `game_record` 表
```sql
-- 添加赛道关联字段
ALTER TABLE `game_record` 
ADD COLUMN `track_id` int(11) DEFAULT 0 COMMENT '赛道ID' AFTER `game_record_id`,
ADD COLUMN `is_passed` tinyint(1) DEFAULT 0 COMMENT '是否通关（0-未通关，1-已通关）' AFTER `clearance_status`,
ADD INDEX `idx_gamer_track` (`gamer`, `track_id`);
```

---

## 三、其他需要考虑的事项

### 1. 前端页面结构设计

#### 需要创建的页面：
- ✅ **首页/赛道选择页** (`pages/game/tracks/index.vue`)
  - 展示四大赛道卡片
  - 显示赛道解锁状态
  - 显示赛道完成进度

- ✅ **赛道关卡列表页** (`pages/game/levels/index.vue`)
  - 展示该赛道下所有关卡
  - 显示关卡解锁状态（锁定/解锁/已完成）
  - 关卡难度标识

- ✅ **游戏交互页面** (`pages/game/play/index.vue`)
  - 结绳计数交互组件
  - 筹算交互组件
  - 珠算交互组件
  - 游戏计时、步数统计
  - 提交答案按钮

- ✅ **通关结果页** (`pages/game/result/index.vue`)
  - 显示通关结果
  - 解锁勋章动画
  - 文化科普弹窗
  - 语音鼓励播放

- ✅ **勋章展示页** (`pages/game/medals/index.vue`)
  - 展示所有勋章
  - 显示已获得/未获得状态

### 2. 交互组件设计

#### 结绳计数组件 (`components/game/RopeCounting.vue`)
- 绳子可视化
- 点击/拖拽打结交互
- 结的数量显示
- 数字对应关系展示

#### 筹算组件 (`components/game/CountingRods.vue`)
- 算筹可视化
- 拖拽排列交互
- 不同位置代表不同数值
- 数字计算展示

#### 珠算组件 (`components/game/Abacus.vue`)
- 算盘可视化
- 点击/拖拽算珠交互
- 上下珠位置计算
- 实时数字显示

### 3. 状态管理（Vuex）

#### 需要新增的 store modules：
```javascript
// store/modules/game.js
- tracks: 赛道列表
- currentTrack: 当前赛道
- levels: 关卡列表
- unlockStatus: 解锁状态
- medals: 勋章列表
- playerMedals: 玩家勋章
- gameProgress: 游戏进度
```

### 4. 本地存储策略

#### 使用 uni.setStorageSync 存储：
- `game_unlock_status`: 关卡解锁状态（离线可用）
- `game_progress`: 游戏进度（临时）
- `player_medals`: 玩家勋章（缓存）

#### 同步策略：
- 游戏开始时从服务器获取最新解锁状态
- 通关后立即同步到服务器
- 离线状态下使用本地存储

### 5. API 接口设计

#### 需要新增的接口：
```
GET  /api/game/tracks/list          # 获取赛道列表
GET  /api/game/levels/list           # 获取关卡列表（按赛道）
GET  /api/game/unlock/status         # 获取玩家解锁状态
POST /api/game/unlock/unlock         # 解锁关卡
POST /api/game/play/submit           # 提交游戏结果
GET  /api/game/medals/list           # 获取勋章列表
GET  /api/game/medals/player         # 获取玩家勋章
POST /api/game/medals/unlock         # 解锁勋章
GET  /api/game/culture/tips          # 获取文化科普内容
```

### 6. 解锁机制逻辑

#### 关卡解锁规则：
1. **第一关**：默认解锁
2. **后续关卡**：完成上一关卡后自动解锁
3. **跨赛道**：完成前一个赛道所有关卡后解锁下一赛道第一关

#### 实现逻辑：
```javascript
// 检查关卡是否可解锁
function canUnlockLevel(levelId, playerUnlockStatus) {
  // 1. 检查是否已完成上一关卡
  // 2. 检查是否满足其他解锁条件
  // 3. 更新解锁状态
}
```

### 7. 勋章系统逻辑

#### 勋章解锁触发时机：
- 完成特定关卡
- 完成整个赛道
- 完成所有赛道
- 达到特定分数
- 连续通关N关

#### 实现逻辑：
```javascript
// 检查并解锁勋章
function checkAndUnlockMedals(gameResult) {
  // 1. 根据游戏结果检查勋章条件
  // 2. 解锁符合条件的勋章
  // 3. 播放解锁动画
  // 4. 保存到数据库
}
```

### 8. 文化科普弹窗

#### 触发时机：
- 完成赛道第一关
- 完成赛道最后一关
- 随机触发（可选）

#### 实现组件：
```vue
<CultureTipModal 
  :tip="cultureTip"
  :image="tipImage"
  @close="closeTip"
/>
```

### 9. 语音反馈功能

#### 需要准备的音频文件：
- `success.mp3` - 通关成功
- `encourage_1.mp3` - 鼓励语1
- `encourage_2.mp3` - 鼓励语2
- `error.mp3` - 错误提示

#### 实现方式：
```javascript
// 使用 uni.createInnerAudioContext()
const audio = uni.createInnerAudioContext();
audio.src = '/static/audio/success.mp3';
audio.play();
```

### 10. UI/UX 设计要点

#### 低龄儿童适配：
- ✅ **大按钮**：按钮尺寸至少 44x44px
- ✅ **鲜艳色彩**：使用明亮、对比度高的颜色
- ✅ **简单图标**：使用卡通、易懂的图标
- ✅ **无密集文字**：文字大号、简洁
- ✅ **即时反馈**：操作后立即有视觉/听觉反馈
- ✅ **容错设计**：错误操作有温和提示，可撤销

#### 交互优化：
- 拖拽操作添加磁吸效果
- 点击操作添加震动反馈（可选）
- 动画过渡流畅自然
- 加载状态友好提示

### 11. 性能优化考虑

- **图片资源**：压缩图片，使用 WebP 格式
- **代码分包**：游戏页面使用分包加载
- **数据缓存**：合理使用缓存减少请求
- **动画优化**：使用 CSS3 动画，避免频繁重绘

### 12. 测试要点

- ✅ 关卡解锁逻辑测试
- ✅ 勋章解锁条件测试
- ✅ 游戏交互流畅性测试
- ✅ 不同设备适配测试
- ✅ 离线状态处理测试
- ✅ 数据同步测试

---

## 四、实施优先级建议

### 第一阶段（核心功能）
1. ✅ 创建赛道表和修改 game_levels 表
2. ✅ 创建关卡解锁状态表
3. ✅ 实现赛道选择页面
4. ✅ 实现关卡列表页面
5. ✅ 实现基础解锁逻辑

### 第二阶段（交互功能）
1. ✅ 实现结绳计数交互组件
2. ✅ 实现筹算交互组件
3. ✅ 实现珠算交互组件
4. ✅ 实现游戏结果提交

### 第三阶段（增强功能）
1. ✅ 实现勋章系统
2. ✅ 实现文化科普弹窗
3. ✅ 实现语音反馈
4. ✅ 优化UI/UX

---

## 五、数据库迁移脚本

建议创建一个完整的 SQL 迁移脚本文件，包含所有表结构的创建和修改。
